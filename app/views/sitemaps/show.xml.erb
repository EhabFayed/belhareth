<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
<% @entries.each do |entry| %><% [entry[:en], entry[:ar]].each do |loc| %>
  <url>
    <loc><%= loc %></loc>
    <xhtml:link rel="alternate" hreflang="en" href="<%= entry[:en] %>"/>
    <xhtml:link rel="alternate" hreflang="ar" href="<%= entry[:ar] %>"/>
<% if entry[:lastmod] %>    <lastmod><%= entry[:lastmod].iso8601 %></lastmod>
<% end %>    <changefreq><%= entry[:changefreq] %></changefreq>
    <priority><%= entry[:priority] %></priority>
  </url>
<% end %><% end %>
</urlset>
